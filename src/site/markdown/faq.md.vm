## Velocity treats double hashes as comments, so $H can be used instead
#set( $H = '#' )
# FAQ

- [Can I not use the project artifacts at all?](#q1)
- [How to exclude automatic modules?](#q2)

$H$H$H <a name="q1" />Can I not use the project artifacts at all?

To exclude the project output from modulepath:
```xml
  <modulepath>
    <dependencysets>
      <dependencyset>
        <includeoutput>false</includeoutput>
      </dependencyset>
    </dependencysets>
  </modulepath>
```

$H$H$H <a name="q2" />How to exclude automatic modules?

To exclude automatic modules from modulepath (but it's a default behavior):
```xml
  <modulepath>
    <dependencysets>
      <dependencyset>
        <excludeautomatic>true</excludeautomatic>
      </dependencyset>
    </dependencysets>
  </modulepath>
```
